# Amazon→美客多 CBT 上架 ERP — 交接文档（2026-09-08）

> 本文件记录 2026-09-07 ~ 2026-09-08 阶段全部修改与遗留事项，供 Codex 接手后直接续做。

## 1. 环境与访问

| 项 | 值 |
|---|---|
| 服务器 | root@8.148.227.139（阿里云，2核 / 1.6G 内存，**内存很紧**） |
| SSH 密钥 | `D:\Desktop\api\美客多.pem`（本机） |
| 代码目录 | /srv/amazon-meli-publisher |
| 域名 | https://ml-erp.woxq.cn |
| Git 分支 | `codex/ozon-ssh-handoff` → GitHub `git@github.com:leeqiang974-alt/mercado-libre-erp.git` |
| DB | 容器 amazon-meli-publisher_postgres_1；psql -U meli -d amazon_meli；密码 Lq830107 |
| 后端容器 | amazon-meli-publisher_backend_1（uvicorn :8000，**前端 dist 由 nginx/静态服务挂载**） |
| 前端构建 | 服务器 node v12 跑不了 tsc/vite，必须 `/opt/node20/bin/node node_modules/vite/bin/vite.js build`；tsc 用 `/opt/node20/bin/node node_modules/typescript/bin/tsc --noEmit` |
| 发布模式 | 店铺#3"晨"（seller 3630453694）= UP（user_product）；店铺#2"轩星"（2942677449）= 传统 |

> 2026-09-08 交接校验：服务器可正常访问 GitHub，远端分支 HEAD 为 `42e43dc`。当前本机直连 GitHub SSH 返回 `Permission denied (publickey)`，在本机 SSH key 修复前，需由服务器仓库执行 GitHub push，并用本机 `production` remote 回拉核对。

## 2. 本阶段提交链（近 → 远，全部已 push）

| commit | 内容 | 涉及文件 |
|---|---|---|
| 22adb83 | worker healthcheck 修复：compose 给 3 个 worker 显式加 pgrep healthcheck（原继承 backend 镜像 curl :8000 永远 unhealthy 18h） | docker-compose.yml |
| 41f0086 | 发布/预检保存失败时**保留具体缺失提示**，不再覆盖成"配置保存失败，未提交发布" | CbtGlobalPublishingPanel.tsx |
| fcdfe16 | 变体区"本页可编辑"不再误标在 selected 变体上（父草稿打开时 selected 变体显示不可点标签、无法建独立草稿）；仅当前草稿绑定 ASIN 才显示，其余一律可点 | CbtGlobalPublishingPanel.tsx |
| c804a49 | 变体列表"独立草稿"从静态 span 改为**可点击按钮**（行为同"编辑此变体"：采集变体页+建独立草稿） | CbtGlobalPublishingPanel.tsx |
| df06e5d | **oss 镜像配对 bug**：by_source 的 key 是高清化后 URL，原用原始 URL 配对导致 mirrored 恒空 → 误报"没有符合500×500px要求"。改为按高清化 URL 配对。实测 #1138（9 张无标记原图）通过 | backend/app/services/storage/aliyun_oss.py |
| d3c1b85 | **提交=页面内容**：预检/发布/合并变体前**无条件保存当前页面**（原仅 !saved 才保存，已保存后改字段不点保存 → 用库旧数据发布） | CbtGlobalPublishingPanel.tsx |
| eb9dbb8 | 无刊登配置草稿（#1128 类）打开时同步草稿自身 price + 给 SELLER_SKU/BRAND/MODEL 默认；保存失败提示**列出全部缺失项**（原只显示第一条） | CbtGlobalPublishingPanel.tsx |
| 027f875 | 草稿保存 title 校验 60→200（validator 与 Field 对齐）；超 60 合规拦截移到**预检环节**（preflight-batch 新增标题长度检查） | backend/app/schemas/drafts.py, routes/publishing.py |
| 5a6366c | 去掉 AI 生成"已生成过"409 门禁——允许再次生成（用户明确要求，超 60 走更强 prompt 重试） | backend/app/services/ai_content_generation.py |
| b399cc3 | 已保存 config 的 global_title/description 为空时回退草稿自身值（修"输入框空但 AI 提示已生成"循环） | CbtGlobalPublishingPanel.tsx |
| 7123559 | 分类智能匹配改用 **Amazon 完整原标题**（174 字符级不截断；打开草稿读 source.snapshot.title，分类搜索框显示完整标题） | CbtGlobalPublishingPanel.tsx |
| 4af6f61 | **多维变体（Color×Size）维度错位**：variationValues key 顺序（size 先）≠ dimensionValuesDisplayData 值数组顺序（DOM 顺序 Color 先），原 zip 把 Color 赋给 Size；改为按页面 variation_ 组出现顺序对齐 | backend/app/services/amazon/parser.py |
| 8d55c14 | **打开变体草稿自动采集变体页**：检测草稿仍绑定父链接 source 时自动建采集任务+插件采集+完成自动切变体草稿（人工"采"保留为二次修复） | CbtGlobalPublishingPanel.tsx |
| 94245df | 变体草稿点"采"改采**变体页**而非父链接（同"编辑此变体"协议：createSourceVariantCollectionJob+插件事件+自动切换） | CbtGlobalPublishingPanel.tsx |
| 54536b9 | AI 标题生成按用户口径：首次 prompt 要求 ≤50 字符，超 60 自动轮询重试（最多 3 次，追加 CORRECTION 压缩指令）。实测 #1129 61→34 字符 | backend/app/services/ai_content_generation.py |
| 4eb04b2 | 草稿标题上限 60→200（与表 varchar(200) 一致）。修超长标题导致 list_drafts 500、上架库显示 0 个 | backend/app/schemas/drafts.py |
| a69fbb7 | 预检返回店铺发布模式（user_product/traditional）；前端提示 UP 模式限制（每 UP 仅 1 个 listing）；错误中文翻译（User products only support 1 item / listing.conflict / meli_global_publish_failed） | publishing.py 等 |
| f927f07 | 商品管理页：标题两行（line-clamp:2）+ 商品列加宽 27% + 净收益/销量右移 | store-products 相关 |
| 875fef2 | 商品管理页净收益/价格非数字显示 "-"（美客多偶返 price:"unknown"） | 前端 |
| c864ca2 / 9db781f | 发布后回查美客多 item 真实状态（active/paused/标题）同步到 ERP；任务列表"刷新美客多状态"按钮 | publishing.py + 前端 |
| 4d5613c | UP 发布 family_name 改用草稿 AI 英文标题（默认 xy 代号被美客多判"标题与图片不匹配"）；批量修复 12 个已发布 Xy 代号 item | publishing.py |
| 1efe61c | 插件回报变体页后同步草稿标题/描述为变体页真实数据 | 前端 |
| 58dcd00 | 变体采集改走本机插件（照搬"采"按钮协议）——创建 browser_extension 任务+预建变体占位 source+草稿，插件回报后更新 | 前后端 |
| dd9ce9c | 镜像 OSS 前自动升级 Amazon 图 1500px 高清+剔除视频占位图（修 pictures.invalid 尺寸拒绝）；新增 publish_worker 拉起脚本（该容器此前缺失导致发布卡 PENDING） | aliyun_oss.py 等 |
| 570a234 | "编辑此变体"先采集变体页真实数据再建草稿；失败回退父页数据并提示；Amazon goto 超时 30s→45s | 前后端 |
| b67ab60 | 变体无独立图时提示更明确（建草稿暂用父页图库，建议补采） | 前端 |
| 6d8c72d | 锁定 Amazon 英文渲染（lc-main=en_US + i18n-prefs=USD cookie），修大陆 IP 采集到中文标题 | collection 相关 |
| 4c2f25e | 保存/预检/发布流程打通：预检与发布自动先保存并自动预检 | 前端 |
| db00d69 | AI 生成链路健壮化：宽容 JSON 解析、中文参考清洗、输出规范化、英文校验改语言级 | ai_content_generation.py |
| 17b2407 | 侧边栏 **AI 模型选择器**：DeepSeek/火山/Agnes 手动切换（DB 持久化、配置驱动可扩展） | 前端+后端 |

## 3. 存量数据修复（SQL 直改，无 commit）

- **source 7327（DrainWig 父链接）**：variants_json 6 → **9 个变体全量** + 完整 Color×Size 属性（原采集漏 Silver Flower 12-Count / Silver 4-Count / White Ocean 4-Count 三个，且属性残缺）。完整矩阵已按真实页面数据写入。**其他历史 source 的 variants_json 不会自动重建**，如需修正需重采或手动 SQL。
- **#904（CBT5173511210）**：4 张带英文文字标签的图用 image_edit 去字后重新上传（POST /pictures 必须 `{"source": url}` 单数，"sources" 数组 400）→ active。

## 4. 关键遗留问题（Codex 接手优先处理）

1. **worker 镜像损坏（紧急）**：已实测 `mvp-skeleton-backend:latest` 的镜像配置错误声明了 `Volumes={"/app/alembic":{},"/app/alembic.ini":{},"/app/app":{},...}`；其中 `/app/alembic.ini` 是文件却被当成 volume，**连最小 `docker run --rm` 都失败**（`cannot mount volume over existing file ... /app/alembic.ini`）。当前 Dockerfile 没有 VOLUME 指令，因此应当重建出不带这些 Config.Volumes 的新镜像，不能继续沿用现有 tag。当前实际状态：
   - `collection_worker` 和 `publish_worker` 是旧容器，已运行约 20 小时；它们仍使用 curl `localhost:8000/health` 的旧 healthcheck，所以显示 unhealthy，但进程仍在运行。
   - `review_worker` **当前不存在/未运行**，不能误写成三个 worker 都在跑。
   - `22adb83` 已将新容器 healthcheck 改为 `pgrep -f app.worker`，但只有成功重建镜像和 worker 后才会生效。
   - 安全处理顺序：先备份/保留当前 tag → 用 `backend/Dockerfile` 构建新 tag → 用 `docker image inspect` 确认 Config.Volumes 不再包含 `/app/alembic.ini` → 最小 `docker run` 验证 → 再逐个重建 worker；不要 `docker-compose down -v`。
2. **服务器内存告急**：1.6G 总内存同时跑 meli + ozon-erp；2026-09-08 校验时 available 约 276MB，无 Swap，有 OOM 风险。建议：先建 Swap/升级内存，再进行镜像重建；并清理确认无用的镜像/容器，给 worker 设置资源限制。
3. **前端需强刷**：本阶段 8 个前端 commit 均需用户 Ctrl+F5 或 `?v=30`→`v=40` 强制刷新才会加载新 JS（当前 dist 已是最新 index-CEf5f-OX.js）
4. **未决事项**：
   - CBT4280039655 与 CBT5173511210 同一产品双 listing（UP 重复），建议下架一个，用户未拍板
   - LLM 模型选择器：后端 provider 已支持（get_current_provider: deepseek/volcengine/agnes），前端选择器已有（17b2407），确认 UI 是否满足"后期可加新模型"
   - 上架库 1080 草稿 2MB 全量加载慢，分页/虚拟滚动未做
   - 用户偏好：①每次修改必须带迭代信息；②交付必须真验证（curl/回读/build/实测），"没执行完成就说完成"会被批评；③变体打开即自动采集（8d55c14）已交付，等验收

## 5. 操作规范（踩过的坑）

- **前端 build**：`cd /srv/amazon-meli-publisher/frontend && /opt/node20/bin/node node_modules/vite/bin/vite.js build`（node v12 直接 SyntaxError）
- **SSH 内联 SQL**：引号常被 PowerShell 破坏 → 写 .sql 到本地 → scp /tmp → docker cp → psql -f
- **curl 传 JSON**：用 `--data @file`（内联反斜杠转义会 400 json_invalid）
- **插件采集协议**：前端 dispatch `meli-amazon-recollect`（detail {sourceProductId, sourceUrl}）→ 本机插件打开页面采集 → 回报 `meli-amazon-recollect-result`（detail {sourceProductId, ok, error}），30s 超时 + 轮询 content_version 兜底
- **变体维度数据**：DrainWig 系列完整 9 变体矩阵（B013FA6NJS=White Flower 2Count / B07SPYHZGF=White Ocean 2Count / B088HGRF1L=Silver Flower 2Count / B08G9ZW9B4=Silver Flower 12Count / B07YBHYRYD=White Ocean 4Count / B07MTGNBDJ=White Flower 4Count / B08GBDK7YH=Silver Flower 4Count / B086537PK4=White Flower 12Count / B089NCHBZG=White Bathtub 2Count）
- **已验证死路**：①root 级 family_name PUT 200 不生效，必须 listing_sites[].family_name；②POST /pictures "sources" 数组 400，必须 {"source": url}；③PUT /items/{CBTid} pictures 400，图只能 PUT /global/user-products/{UP} 带 pictures:[{id}]；④git add frontend/dist 被 .gitignore
- **服务器工作树**：2026-09-08 校验时有一个未跟踪备份 `docker-compose.yml.bak-20260908`；不影响运行，但交接时不应误报为完全 clean。
